name: daily-cron
on:
  schedule:
    - cron: '0 12 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Run Rubbish Scheduler on Vercel
        run: |
          curl --request GET \
          --url 'https://rubbish-scheduler.jonur.io/api'
